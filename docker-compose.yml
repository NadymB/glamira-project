version: "3.9"

services:
  producer:
    build: .
    command: python etl/extract_data.py
    env_file:
      - .env

  worker:
    build: .
    command: python src/ingestion/worker.py
    env_file:
      - .env
    restart: always

  consumer:
    build: .
    command: python etl/load_data.py
    env_file:
      - .env
    restart: always